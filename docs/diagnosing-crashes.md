Moved to `E:\Tools\mods\cp77\proj\_shared\diagnosing-crashes.md` — the crash-report triage process (REDscope, WinDbg, the engine's own `attch/` breadcrumb file, the stale-compile-cache check) isn't specific to this project, just written up here first. Read it there; this stub exists so old links into `docs/` don't dead-end.
