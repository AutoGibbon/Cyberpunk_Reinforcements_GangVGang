# Crash: 2026-08-30 12:45:38 (raw dump, no REDscope)

Source: `Cyberpunk2077-rawcrashdumps/Cyberpunk2077-20260830-124538-5456-23632/`
PID 5456, crashing TID 23632. Process uptime at crash: 0:35:02 (longest
surviving session in this batch).

## Raw evidence

- `stacktrace.txt`: read of `0x8` — same bad address as the 10:01:10
  crash, but:
- WinDbg `!analyze -v`: fault at `Cyberpunk2077+0x149157` — a **different
  offset** from the 10:01:10 crash (`+0x1dab43`).
- `.ecxr`: `mov rcx, qword ptr [r8+8]` with `r8=0` — reading the field at
  offset 8 of a null object (feeding it into `rcx`, presumably as an
  argument to the next call), not the vtable-call shape seen at 10:01:10.
- Manual stack walk resolves three further frames
  (`Cyberpunk2077+0x2b150d8`, `+0x14a055`, `+0x2abbed8`) before running out
  of captured memory.
- **Engine breadcrumb file**: no `SystemUpdate/DebugName` entry present
  for TID 23632.
- `"Game/LoadingStage":"Finished"` — mid-gameplay.

## Isolated assessment

Shares only the bad *address* (`0x8`, itself a generic "null object, read
near the start of it" pattern) with the 10:01:10 crash — the code offset,
instruction, and register shapes are all different, and this one has no
system breadcrumb (unlike 10:01:10's clean `QuestsSystem/Tick` hit), so
there's no basis to treat them as the same bug. This should be logged as
its own, unattributed single-occurrence fault.

Nothing in the captured registers or stack (no recognizable pointers into
this mod's known data shapes, no matching breadcrumb) implicates
`Reinforcements_GangVsGang`. Can't rule it in or out further without a
REDscope capture, which wasn't installed yet at this timestamp.
