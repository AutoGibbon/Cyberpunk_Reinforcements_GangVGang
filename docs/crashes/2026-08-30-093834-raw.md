# Crash: 2026-08-30 09:38:34 (raw dump, no REDscope)

PID 3624, crashing TID 14820. Uptime 0:05:04.

- `stacktrace.txt`: read of `0xFFFFFFFFFFFFFFFF` (all-ones sentinel).
- `!analyze -v`: fault at `Cyberpunk2077+0x53f9d4`.
- `.ecxr`: `mov rdi, qword ptr [r8]`, `r8=0x3f8000004125869b` — high/low
  32-bit halves decode as floats `1.0` / `≈10.35`: packed floats
  reinterpreted as a pointer (type confusion), not a plain null.
- No `SystemUpdate/DebugName` breadcrumb for TID 14820.
- `Game/LoadingStage: Spawning player`, `IsLoadingSavedSession: true` —
  load-time, during save load, not gameplay.

This mod has no code on the player-spawn path. Different offset from
[094418](2026-08-30-094418-raw.md) despite the same bad address — treated
as a separate, unattributed fault. See
[crash-audit-2026-08-30.md](../crash-audit-2026-08-30.md).
