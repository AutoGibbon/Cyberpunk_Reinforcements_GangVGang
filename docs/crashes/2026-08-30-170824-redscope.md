# Crash: 2026-08-30 17:08:24 (REDscope-instrumented)

`REDscope-2026-08-30_17-08-24.crash`, CRASH ID `HJBGKZX7` (same
fingerprint-derived ID as the 15:35:10 report behind
`crash-audit-2026-08-29.md` finding #2 — not the same literal event, see
the CRASH ID note in
[../../_shared/diagnosing-crashes.md](../../_shared/diagnosing-crashes.md)).
Uptime ~10 min. Game launched 15:28:16 — many hours after `d8ac682`
(finding #2's fix) and the 3.1.1 changelog.

- Same fingerprint as the whole cluster: read at `0x3258`,
  `Cyberpunk2077.exe+0x2F098B`.
- **Clean, direct RedScript attribution**: `SpawnRequestFinished;
  DSSSpawnRequestResult on gameDynamicSpawnSystem`, depth 1.
- Live state: `combat: no`, `district: Westbrook / Japantown`.

**Confirmed directly by the user**: this build already included finding
#10's `GetPuppetStateBlackboard()` guard (uncommitted at the time) — so
finding #10 is ruled out as this crash's cause, and so is a stale pre-fix
build (finding #2's fix was live for hours).

Two remaining gaps found and fixed this session, in the same
`SpawnRequestFinished`:

- Vehicle-command loop: `wheeledObject.GetAIComponent().SetInitCmd(...)`
  (two call sites) and `wheeledObject.GetVehicleComponent().ToggleSiren(...)`
  chained with no `IsDefined()` check on the accessor result. CDPR's own
  code null-checks `GetVehicleComponent()`'s result elsewhere
  (`vehicleComponent.reds:5125`); vanilla's own `SpawnCallback` chains
  `GetAIComponent()` unguarded too — the trap this mod copied.
- First loop: `spawnedObject` used in `.IsPuppet()`/`.IsVehicle()` with no
  `IsDefined()` check, unlike the sibling `SpawnCallback` method which
  guards its own parameter.

Both fixed. Neither fully proven against this exact fingerprint (no
further crash landed to confirm), but both are real gaps in the crashing
build. See [crash-audit-2026-08-30.md](../crash-audit-2026-08-30.md) for
what came next.
