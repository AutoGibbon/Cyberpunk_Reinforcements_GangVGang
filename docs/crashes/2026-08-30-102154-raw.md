# Crash: 2026-08-30 10:21:54 (raw dump, no REDscope)

Source: `Cyberpunk2077-rawcrashdumps/Cyberpunk2077-20260830-102154-9508-9840/`
PID 9508, crashing TID 9840. Process uptime at crash: 0:20:14.

## Raw evidence

- `stacktrace.txt`: read of `0x3258` — same address as the 09:25:57 crash
  in this batch.
- WinDbg `!analyze -v`: fault at `Cyberpunk2077+0x2f098b` — **identical
  offset** to the 09:25:57 crash too.
- `.ecxr` register/instruction dump: `mov al, byte ptr [rbx]`, with
  `rbx=rcx=0x3258`, `rdi=0`, `rbp=0`, `r12=0`, `r13=0`, `r14=0xFFFFFFFF`,
  `rsi=0x00007ff719e2b200` — **every register matches the 09:25:57 crash
  exactly**, including `rsi` holding the identical captured-module pointer.
- Manual stack walk shows the same resolvable caller return address,
  `Cyberpunk2077+0x12CB2FE`, as the 09:25:57 crash.
- **Engine breadcrumb file**:
  ```
  "SystemUpdate/UpdateTickGroup@...#TID=9840":"MappinsUpdate"
  "SystemUpdate/DebugName@...#TID=9840":"DynamicSpawnSystem/Update"
  ```
  Same system as the 09:25:57 crash: `DynamicSpawnSystem/Update`.
- `"Game/LoadingStage":"Finished"` — mid-gameplay.

## Isolated assessment

Byte-for-byte identical fault (instruction, full register set, resolvable
caller address, and the engine's own tick-system breadcrumb) to the
09:25:57 crash in this same batch. This is not "the same kind of bug" —
this is the *same call happening again*. See the batch summary for how
this lines up with `crash-audit-2026-08-29.md` finding #2
(`SpawnRequestFinished` unguarded `ScriptedPuppet` cast in
`DynamicSpawnSystem.reds`).
