# Crash: 2026-08-30 09:25:57 (raw dump, no REDscope)

Source: `Cyberpunk2077-rawcrashdumps/Cyberpunk2077-20260830-092557-13268-24244/`
PID 13268, crashing TID 24244. Process uptime at crash: 0:02:35 (crashed
very shortly after launch/load).

## Raw evidence

- `stacktrace.txt`: `EXCEPTION_ACCESS_VIOLATION`, read of `0x3258`.
- WinDbg `!analyze -v`: `FAILURE_BUCKET_ID: INVALID_POINTER_READ_c0000005_Cyberpunk2077.exe!Unknown`,
  fault at `Cyberpunk2077+0x2f098b`, `FRAME_ONE_INVALID: 1` (can't unwind
  past frame 0 from the minidump alone).
- `.ecxr` register/instruction dump: `mov al, byte ptr [rbx]` with
  `rbx=rcx=0x3258`, `rdi=0`, `rbp=0`, `r12=0`, `r13=0`, `r14=0xFFFFFFFF`,
  `rsi=0x00007ff719e2b200`. Manual stack walk from `.ecxr` shows the
  immediate caller's return address as `0x00007ff7`19e2b2fe`` (i.e.
  `Cyberpunk2077+0x12CB2FE`).
- **Engine's own crash-info breadcrumb** (`attch/Cyberpunk2077.exe-...txt`,
  written by the game itself, independent of REDscope/WinDbg):
  ```
  "SystemUpdate/UpdateTickGroup@...#TID=24244":"MappinsUpdate"
  "SystemUpdate/DebugName@...#TID=24244":"DynamicSpawnSystem/Update"
  ```
  This directly names the native system ticking on the crashing thread at
  the moment of the fault: `DynamicSpawnSystem/Update`.
- `"Game/LoadingStage":"Finished"`, `"Engine/Scripts/Loaded":"true"` — this
  was mid-session gameplay, not a load-time crash.

## Isolated assessment

Fault signature (instruction, full register set, and the one resolvable
caller return address) is a null-pointer byte-read at object-offset
`0x3258`, reached through `DynamicSpawnSystem/Update`. This is consistent
with a spawn-batch iteration dereferencing a bad object — the exact shape
of `crash-audit-2026-08-29.md` finding #2 (`SpawnRequestFinished` casting
to `ScriptedPuppet` without checking the cast). Taken alone (no REDscope
script-level attribution available for this specific dump), this is
strong-but-not-proof evidence for finding #2; see the cross-referenced
summary for corroboration against the other dumps in this batch.

Nothing in the loaded-module list or breadcrumb data suggests a different
mod or engine subsystem is the culprit.
