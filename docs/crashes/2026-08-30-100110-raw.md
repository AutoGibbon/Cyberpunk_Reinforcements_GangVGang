# Crash: 2026-08-30 10:01:10 (raw dump, no REDscope)

Source: `Cyberpunk2077-rawcrashdumps/Cyberpunk2077-20260830-100110-9468-25176/`
PID 9468, crashing TID 25176. Process uptime at crash: 0:15:09.

## Raw evidence

- `stacktrace.txt`: read of `0x8` (null object + 8-byte offset — the
  classic "first field / vtable slot of a null object" signature).
- WinDbg `!analyze -v`: fault at `Cyberpunk2077+0x1dab43`.
- `.ecxr`: `call qword ptr [rax+8]` with `rax=0` — this is a **virtual
  call through a null object pointer** (calling whatever sits at
  vtable-plus-one-slot on a null `this`), not a plain field read.
- Manual stack walk resolves several more frames than most dumps in this
  batch (`Cyberpunk2077+0x25771f` visible two frames up).
- **Engine breadcrumb file** — this one has a clean hit:
  ```
  "SystemUpdate/UpdateTickGroup@...#TID=25176":"PreBuckets"
  "SystemUpdate/DebugName@...#TID=25176":"QuestsSystem/Tick"
  ```
  The crashing thread was inside the vanilla **`QuestsSystem/Tick`**, not
  any spawn/reinforcement system.
- `"Game/LoadingStage":"Finished"` — mid-gameplay.

## Isolated assessment

This is a null virtual-call crash inside CDPR's own quest system tick,
confirmed directly by the engine's own breadcrumb — not inference. Nothing
in `Reinforcements_GangVsGang` touches `QuestsSystem` or anything
downstream of it. This crash can be ruled out as unrelated to this mod
with high confidence, on first-party evidence rather than a guess.

Worth flagging to the user as a separate, vanilla/other-mod issue if it
recurs — but out of scope for this mod's crash audit.
