# Crash: 2026-08-30 09:44:18 (raw dump, no REDscope)

Source: `Cyberpunk2077-rawcrashdumps/Cyberpunk2077-20260830-094418-23640-13644/`
PID 23640, crashing TID 13644. Process uptime at crash: 0:05:26. Only six
minutes after the previous crash in this batch (09:38:34) — same relaunch
loop.

## Raw evidence

- `stacktrace.txt`: read of `0xFFFFFFFFFFFFFFFF` — same bad-address
  sentinel as the 09:38:34 crash.
- WinDbg `!analyze -v`: fault at `Cyberpunk2077+0x1f571a` — **a different
  offset** from the 09:38:34 crash (`+0x53f9d4`), despite the same bad
  address.
- `.ecxr`: `mov eax, dword ptr [rcx+18h]` with `rcx=0xc328000000020000`.
  Again not a clean pointer — high 32 bits `0xc3280000` decode as float
  `-168.0`, another packed-float-as-pointer shape rather than a plain null.
- Manual stack walk yields no additional resolvable frames.
- **Engine breadcrumb file**: no `SystemUpdate/DebugName` entry for TID
  13644.
- `"Game/LoadingStage":"Spawning player"`,
  `"Game/SessionDesc/IsLoadingSavedSession":"true"` — again a load-time
  crash during player spawn, not gameplay.

## Isolated assessment

Same profile as 09:38:34: crashes during save-load/player-spawn, reading a
garbage (float-bit-pattern) value through a pointer, no engine breadcrumb
tying it to any tick system. The *different* code offset from the
09:38:34 crash (despite sharing the same bad address) means these two are
best treated as two separate faults that happen to produce the same
"all-ones" sentinel, not one repeating bug — same reasoning the diagnosing
process notes call out: address alone is weak evidence without a matching
offset.

`Reinforcements_GangVsGang` has no code path active during player spawn/
load, so nothing here implicates this mod. Unattributed without a REDscope
capture; see the batch summary for how this pairs with the 09:38:34 crash.
